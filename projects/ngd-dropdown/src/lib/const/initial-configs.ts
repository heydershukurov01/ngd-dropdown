import {NgdDropdownConfigs} from "../ngd-dropdown";

export const InitialConfigs: NgdDropdownConfigs = {
  theme: 'default',
  placeholder: 'Select',
  multiple: true,
  jsSearch: true,
  serverSearch: true,
  searchTimeout: 200,
  option: {
    name: 'title',
    value: 'title'
  }
};
