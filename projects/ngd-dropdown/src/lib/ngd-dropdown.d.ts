export interface NgdDropdownConfigs {
  theme: 'default',
  placeholder: string,
  multiple: boolean,
  jsSearch: boolean,
  serverSearch: boolean,
  searchTimeout: number,
  option: {
    name: string,
    value: string
  }
}
